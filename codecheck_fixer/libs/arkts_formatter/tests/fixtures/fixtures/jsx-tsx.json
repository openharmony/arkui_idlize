{
  "equivalent": [
    {
      "description": "jsx with many props",
      "original": "const element = <Button onClick={handleClick} disabled={isDisabled} className=\"primary\" style={{ color: 'blue' }} data-testid=\"submit-button\" />;",
      "expected": "const element = <Button onClick={handleClick} disabled={isDisabled} className=\"primary\" style={{ color: 'blue' }} data-testid=\"submit-button\" />;"
    },
    {
      "description": "jsx with children",
      "original": "const component = <Container className=\"wrapper\"><Header title=\"Title\" /><Content>{data.map(item => <Item key={item.id} {...item} />)}</Content></Container>;",
      "expected": "const component = <Container className=\"wrapper\"><Header title=\"Title\" /><Content>{data.map(item => <Item key={item.id} {...item} />)}</Content></Container>;"
    },
    {
      "description": "jsx with spread props",
      "original": "return <Component {...baseProps} {...customProps} additionalProp={value} onEvent={handler} />;",
      "expected": "return <Component {...baseProps} {...customProps} additionalProp={value} onEvent={handler} />;"
    },
    {
      "description": "conditional jsx rendering",
      "original": "return <div>{isLoading ? <Spinner size=\"large\" color=\"blue\" /> : hasError ? <Error message={errorMessage} /> : <Content data={data} />}</div>;",
      "expected": "return <div>{isLoading ? <Spinner size=\"large\" color=\"blue\" /> : hasError ? <Error message={errorMessage} /> : <Content data={data} />}</div>;"
    },
    {
      "description": "jsx with inline styles",
      "original": "const styled = <div style={{ backgroundColor: 'white', padding: '20px', margin: '10px', borderRadius: '8px' }}>Content</div>;",
      "expected": "const styled = <div style={{ backgroundColor: 'white', padding: '20px', margin: '10px', borderRadius: '8px' }}>Content</div>;"
    },
    {
      "description": "jsx fragment with many items",
      "original": "return <>{items.map(item => <ListItem key={item.id} title={item.title} description={item.description} />)}</>;",
      "expected": "return <>{items.map(item => <ListItem key={item.id} title={item.title} description={item.description} />)}</>;"
    }
  ],
  "notEquivalent": []
}
