{
  "equivalent": [
    {
      "description": "object literal properties wrap",
      "original": "const user = { firstName: 'John', lastName: 'Doe', email: 'john.doe@example.com', age: 30, address: '123 Main St' };",
      "expected": "const user = { firstName: 'John', lastName: 'Doe', email: 'john.doe@example.com', age: 30, address: '123 Main St' };"
    },
    {
      "description": "nested objects",
      "original": "const config = { database: { host: 'localhost', port: 5432, credentials: { username: 'admin', password: 'secret' } }, cache: { ttl: 3600 } };",
      "expected": "const config = { database: { host: 'localhost', port: 5432, credentials: { username: 'admin', password: 'secret' } },\n  cache: { ttl: 3600 } };"
    },
    {
      "description": "object with methods",
      "original": "const calculator = { add: (a, b) => a + b, subtract: (a, b) => a - b, multiply: (a, b) => a * b, divide: (a, b) => a / b };",
      "expected": "const calculator = { add: (a, b) => a + b, subtract: (a, b) => a - b, multiply: (a, b) => a * b,\n  divide: (a, b) => a / b };"
    },
    {
      "description": "computed properties",
      "original": "const obj = { [key1]: value1, [key2]: value2, normalKey: normalValue, [computedKey()]: computedValue() };",
      "expected": "const obj = { [key1]: value1, [key2]: value2, normalKey: normalValue, [computedKey()]: computedValue() };"
    },
    {
      "description": "object spread",
      "original": "const merged = { ...defaultConfig, ...userConfig, override: true, timestamp: Date.now(), version: '1.0.0' };",
      "expected": "const merged = { ...defaultConfig, ...userConfig, override: true, timestamp: Date.now(), version: '1.0.0' };"
    }
  ],
  "notEquivalent": []
}
