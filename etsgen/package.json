{
  "name": "@idlizer/etsgen",
  "version": "2.1.5+devel",
  "description": "",
  "main": "lib/main.js",
  "bin": "lib/main.js",
  "files": [
    "lib/*.js",
    "generation-config/**/*",
    "config.json"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "dependencies": {
    "@idlizer/core": "2.1.5+devel",
    "@idlizer/libohos": "2.1.5+devel",
    "@koalaui/libarkts": "file:../external/ui2abc/libarkts",
    "@types/node": "^18.0.0",
    "commander": "^10.0.0",
    "@rollup/plugin-commonjs": "^26.0.1",
    "@rollup/plugin-node-resolve": "^15.3.0",
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/plugin-typescript": "^11.1.6"
  },
  "scripts": {
    "clean": "rimraf -g build",
    "compile": "npm run -C ../core compile && npm run -C ../libohos compile && tsc -b . && rollup -c",
    "run:sdk": "npm run compile && node . --ets2idl --output-dir ./out/idl --base-dir ../sdk-patched-arkts/api --input-dir  ../sdk-patched-arkts/api/arkui --input-files $(cat extended-input.txt | tr '\\n' ' ') && cp stubs.idl out/idl/stubs.idl",
    "run": "npm run compile && node . --ets2idl --output-dir ./out/idl --base-dir ./input --input-dir  ./input --verify-idl --docs=none"
  }
}