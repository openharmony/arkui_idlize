package ohos.base;

callback ErrorCallback = void (BusinessError error);

interface BusinessError {
    attribute String name;
    attribute String message;
    [Optional]
    attribute String stack;

    attribute number code;
    // the `data` attribute does not point with 'void' type
};