export interface BaseSpan {
    width(value: number | undefined): this
}

export interface CommonMethod {
    width(value: number | undefined): this
}

export interface CommonShapeMethod {
    width(value: number | undefined): this
}

export interface ScrollableCommonMethod {
    width(value: number | undefined): this
}